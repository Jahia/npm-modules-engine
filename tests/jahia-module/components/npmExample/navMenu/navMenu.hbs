{{#*inline "menuEntry"}}
<li class="{{menuEntryCss this @first @last}}">
  {{{this.render}}}
  {{#if this.children}}
  <div class="navBar">
  <ul class="inner-box level_{{level}}">
    {{#each this.children}}
      {{> menuEntry}}
    {{/each}}
  </ul>
  </div>
  {{/if}}
</li>
{{/inline}}

<div class="navBar">
  <ul class="navmenu level_0">
  {{#each (buildNavMenu baseNode=currentContent.properties.[j:baselineNode] maxDepth=currentContent.properties.[j:maxDepth] startLevel=currentContent.properties.[j:startLevel])}}
    {{> menuEntry}}
  {{/each}}
  </ul>
</div>